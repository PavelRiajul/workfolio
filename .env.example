# Sanity — copy this file to `.env` and fill in your project values.
# These are publishable values (Astro exposes PUBLIC_* to the browser), so the
# project id is safe to commit. Read/write tokens are NOT — keep those out.
#
# Leave PUBLIC_SANITY_PROJECT_ID as "placeholder" to run fully offline
# (the site serves the seeded fallback content baked into src/data).
#
# Manage the project at https://www.sanity.io/manage
PUBLIC_SANITY_PROJECT_ID=1tfx8i80
PUBLIC_SANITY_DATASET=production
